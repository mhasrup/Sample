@Library('mhasrup-libraries') _
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                example1 "Ajinkya" 
                // Simulate failure
                // error("Build failed") 
            }
        }

    }
}
